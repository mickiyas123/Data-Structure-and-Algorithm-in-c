#include <stdio.h>

/**
 * main - Entry point
 *
 * Decription: This function shows how to get the length of a string
 *             Note: strlen function from string.h library can be used
 *                   to impliment the function below
 * Return: Always 0 successful
 */

int main(void)
{
	char *s = "Something nice";

	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	printf("Lenght of s is %d\n", i);
	return (0);
}
